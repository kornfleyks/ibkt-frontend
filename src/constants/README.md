# IBKT Frontend Constants

Auto-generated from the Monday.com board schema exports provided in ChatGPT.

Copy this `constants` folder into `src/constants` in the React app.

Example:

```js
import { USERS, CATS, ROLES, CATS_STATUS_OPTIONS } from "@/constants";

const passwordColumnId = USERS.COLUMNS.PASSWORD_HASH;
const ready = CATS_STATUS_OPTIONS.STATUS.ADOPTION_READY;
```
